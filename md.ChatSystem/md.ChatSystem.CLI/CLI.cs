namespace Md.ChatSystem.CLI
{
    public class CLI
    {
        public CLI(string[] arguments)
        {
            
        }
    }
}
/*
 * ============== Views ==============
 * Main view: Join server; Host server; Quit;
 *  Server view: Open public server list; Enter server URL; Back;
 *      Public server view: Paged server list (marckable by arrows up/down, 2nd from last - next, last - back if first page, previeos if not)
 *          CHAT FEED: message or command /command (/return - returns to server view, /exit - terminates the program)
 *      Prompt that asks to enter URL or return.
 *          CHAT FEED: ...
 *  Host: TODO
 *      
 */